# ATTO FLOW backups

Production should back up PostgreSQL, Redis snapshots, uploaded files and WhatsApp session directories. The current MVP uses in-memory stores only; this folder documents the target backup location for deploy automation.
